/*

Explain Window Object

1. The Window Object is an object that contains properties and methods for the browser window.
2. The Window Object is created when the browser window is created.
3. The Window Object is destroyed when the browser window is closed.
4. JavaScript engine also creates the (this) keyword.
5. At the global level, the Window Object is equal to (this) keyword. Example: (window) === (this).
6. We can access the variables and functions by using (this) keyword or (window).
*/

// console.log(window === this);

// var numeric = 5;

// function testGlobal() {
//     console.log(`testGlobal`);
// }

// console.log(this.numeric);
// console.log(window.numeric);
// console.log(numeric);

// this.testGlobal();
// window.testGlobal();
// testGlobal();
